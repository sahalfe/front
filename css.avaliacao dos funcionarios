/* Container principal */
.manager-container {
    width: 100%;
    height: 100vh; /* Tela cheia */
    margin: 0;
    padding: 40px;
    background: linear-gradient(to bottom, #000033, #000000); /* Degradê com transparência */
    border-radius: 12px;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
    font-family: 'Inter', sans-serif; /* Fonte Inter */
    color: #fff; /* Texto branco */
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
}

/* Título principal */
h1 {
    font-size: 28px;
    margin-bottom: 25px;
    font-weight: 600;
    letter-spacing: 0.5px;
}

/* Container de busca */
.search-container {
    display: flex;
    justify-content: center;
    margin-bottom: 80px;
}

#search-input {
    width: 100%;
    max-width: 900px; /* Aumentar largura */
    padding: 12px;
    border: 1px solid #444444df;
    border-radius: 8px;
    font-size: 16px;
    background-color: rgba(0, 0, 0, 0.9); /* Cor de fundo mais escura e translúcida */
    color: #fff;
    transition: all 0.3s ease;
}

#search-input:focus {
    border-color: #007bff;
    outline: none;
    background-color: rgba(51, 51, 51, 1); /* Aumenta opacidade no foco */
}

/* Painel de setores */
.sector-panel {
    display: flex;
    flex-direction: column;
    gap: 20px;
    margin-top: 20px;
    width: 100%;
    max-width: 800px; /* Aumentar largura do painel */
}

/* Feedback do funcionário */
.employee-feedback {
    padding: 20px;
    border: 1px solid #444;
    border-radius: 8px;
    background-color: rgba(34, 34, 34, 0.8); /* Transparência no fundo */
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    width: 100%;
    max-width: 800px; /* Aumentar largura */
}

.employee-feedback p {
    margin: 8px 0;
    font-size: 16px;
    color: #ccc;
}

/* Seção de comentários */
.comments-section {
    margin-top: 20px;
}

.comments-section ul {
    margin: 10px 0;
    padding-left: 20px;
    list-style-type: none;
}

.comments-section ul li {
    margin: 8px 0;
    font-size: 14px;
    color: #bbb;
}

/* Botões */
button {
    padding: 12px 20px;
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 30px;
    font-size: 16px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s ease;
}

button:hover {
    background-color: #0056b3;
    transform: translateY(-2px);
}

button:active {
    background-color: #003366;
    transform: translateY(0);
}

/* Adicionando uma transição suave ao passar o mouse nos botões */
button:focus {
    outline: none;
    box-shadow: 0 0 0 2px rgba(0, 123, 255, 0.5);
}

/* Responsividade para telas pequenas */
@media (max-width: 600px) {
    .manager-container {
        padding: 20px;
    }

    #search-input {
        max-width: 100%; /* Ajusta a largura para telas pequenas */
    }

    .sector-panel, .employee-feedback {
        max-width: 100%; /* Ajusta para 100% de largura em telas pequenas */
    }

    @media (max-width: 600px) {
        h1 {
            font-size: 24px; /* Ajusta o tamanho do título em telas menores */
        }
    }
}
